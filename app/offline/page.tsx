import { OfflinePage } from '@/components/offline/offline-page'

export default function Offline() {
  return <OfflinePage />
}

export const metadata = {
  title: 'Offline - MyWallet',
  description: 'You are currently offline. Your data will sync when you reconnect.',
}